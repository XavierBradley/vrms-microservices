spring:
  datasource:
    url: jdbc:postgresql://${POSTGRES_HOST:localhost}:${POSTGRES_PORT:5432}/${POSTGRES_DB:agents_db}
    username: ${POSTGRES_USER:agents}
    password: ${POSTGRES_PASSWORD:agents}

  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect